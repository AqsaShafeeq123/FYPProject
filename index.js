/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app.json';
import stackss from './src/components/ScreensContainer';

AppRegistry.registerComponent(appName, () => stackss);
